package hxcurl.easy;

import hxcurl.CurlOptOffset;

/**
 * Abstract enums wrapping the various libcurl easy option codes.
 *
 * @link http://curl.haxx.se/libcurl/c/curl_easy_setopt.html
 * @link https://github.com/bagder/curl/blob/ade380a7aa1241fd7a2e16ee0c44fe268b42ff9a/include/curl/curl.h
 */
@:enum
abstract CurlOpt(Int) to Int
{
    var WRITEDATA                  = (cast OBJECTPOINT)   + 1;
    var URL                        = (cast OBJECTPOINT)   + 2;
    var PORT                       = (cast LONG)          + 3;
    var PROXY                      = (cast OBJECTPOINT)   + 4;
    var USERPWD                    = (cast OBJECTPOINT)   + 5;
    var PROXYUSERPWD               = (cast OBJECTPOINT)   + 6;
    var RANGE                      = (cast OBJECTPOINT)   + 7;
    var READDATA                   = (cast OBJECTPOINT)   + 9;
    var ERRORBUFFER                = (cast OBJECTPOINT)   + 10;
    var WRITEFUNCTION              = (cast FUNCTIONPOINT) + 11;
    var READFUNCTION               = (cast FUNCTIONPOINT) + 12;
    var TIMEOUT                    = (cast LONG)          + 13;
    var INFILESIZE                 = (cast LONG)          + 14;
    var POSTFIELDS                 = (cast OBJECTPOINT)   + 15;
    var REFERER                    = (cast OBJECTPOINT)   + 16;
    var FTPPORT                    = (cast OBJECTPOINT)   + 17;
    var USERAGENT                  = (cast OBJECTPOINT)   + 18;
    var LOW_SPEED_LIMIT            = (cast LONG)          + 19;
    var LOW_SPEED_TIME             = (cast LONG)          + 20;
    var RESUME_FROM                = (cast LONG)          + 21;
    var COOKIE                     = (cast OBJECTPOINT)   + 22;
    var HTTPHEADER                 = (cast OBJECTPOINT)   + 23;
    var HTTPPOST                   = (cast OBJECTPOINT)   + 24;
    var SSLCERT                    = (cast OBJECTPOINT)   + 25;
    var KEYPASSWD                  = (cast OBJECTPOINT)   + 26;
    var CRLF                       = (cast LONG)          + 27;
    var QUOTE                      = (cast OBJECTPOINT)   + 28;
    var HEADERDATA                 = (cast OBJECTPOINT)   + 29;
    var COOKIEFILE                 = (cast OBJECTPOINT)   + 31;
    var SSLVERSION                 = (cast LONG)          + 32;
    var TIMECONDITION              = (cast LONG)          + 33;
    var TIMEVALUE                  = (cast LONG)          + 34;
    var CUSTOMREQUEST              = (cast OBJECTPOINT)   + 36;
    var STDERR                     = (cast OBJECTPOINT)   + 37;
    var POSTQUOTE                  = (cast OBJECTPOINT)   + 39;
    var VERBOSE                    = (cast LONG)          + 41;
    var HEADER                     = (cast LONG)          + 42;
    var NOPROGRESS                 = (cast LONG)          + 43;
    var NOBODY                     = (cast LONG)          + 44;
    var FAILONERROR                = (cast LONG)          + 45;
    var UPLOAD                     = (cast LONG)          + 46;
    var POST                       = (cast LONG)          + 47;
    var DIRLISTONLY                = (cast LONG)          + 48;
    var APPEND                     = (cast LONG)          + 50;
    var NETRC                      = (cast LONG)          + 51;
    var FOLLOWLOCATION             = (cast LONG)          + 52;
    var TRANSFERTEXT               = (cast LONG)          + 53;
    var PUT                        = (cast LONG)          + 54;
    var PROGRESSFUNCTION           = (cast FUNCTIONPOINT) + 56;
    var PROGRESSDATA               = (cast OBJECTPOINT)   + 57;
    var XFERINFODATA               = CurlOpt.PROGRESSDATA;
    var AUTOREFERER                = (cast LONG)          + 58;
    var PROXYPORT                  = (cast LONG)          + 59;
    var POSTFIELDSIZE              = (cast LONG)          + 60;
    var HTTPPROXYTUNNEL            = (cast LONG)          + 61;
    var INTERFACE                  = (cast OBJECTPOINT)   + 62;
    var KRBLEVEL                   = (cast OBJECTPOINT)   + 63;
    var SSL_VERIFYPEER             = (cast LONG)          + 64;
    var CAINFO                     = (cast OBJECTPOINT)   + 65;
    var MAXREDIRS                  = (cast LONG)          + 68;
    var FILETIME                   = (cast LONG)          + 69;
    var TELNETOPTIONS              = (cast OBJECTPOINT)   + 70;
    var MAXCONNECTS                = (cast LONG)          + 71;
    var FRESH_CONNECT              = (cast LONG)          + 74;
    var FORBID_REUSE               = (cast LONG)          + 75;
    var RANDOM_FILE                = (cast OBJECTPOINT)   + 76;
    var EGDSOCKET                  = (cast OBJECTPOINT)   + 77;
    var CONNECTTIMEOUT             = (cast LONG)          + 78;
    var HEADERFUNCTION             = (cast FUNCTIONPOINT) + 79;
    var HTTPGET                    = (cast LONG)          + 80;
    var SSL_VERIFYHOST             = (cast LONG)          + 81;
    var COOKIEJAR                  = (cast OBJECTPOINT)   + 82;
    var SSL_CIPHER_LIST            = (cast OBJECTPOINT)   + 83;
    var HTTP_VERSION               = (cast LONG)          + 84;
    var FTP_USE_EPSV               = (cast LONG)          + 85;
    var SSLCERTTYPE                = (cast OBJECTPOINT)   + 86;
    var SSLKEY                     = (cast OBJECTPOINT)   + 87;
    var SSLKEYTYPE                 = (cast OBJECTPOINT)   + 88;
    var SSLENGINE                  = (cast OBJECTPOINT)   + 89;
    var SSLENGINE_DEFAULT          = (cast LONG)          + 90;
    var DNS_USE_GLOBAL_CACHE       = (cast LONG)          + 91;
    var DNS_CACHE_TIMEOUT          = (cast LONG)          + 92;
    var PREQUOTE                   = (cast OBJECTPOINT)   + 93;
    var DEBUGFUNCTION              = (cast FUNCTIONPOINT) + 94;
    var DEBUGDATA                  = (cast OBJECTPOINT)   + 95;
    var COOKIESESSION              = (cast LONG)          + 96;
    var CAPATH                     = (cast OBJECTPOINT)   + 97;
    var BUFFERSIZE                 = (cast LONG)          + 98;
    var NOSIGNAL                   = (cast LONG)          + 99;
    var SHARE                      = (cast OBJECTPOINT)   + 100;
    var PROXYTYPE                  = (cast LONG)          + 101;
    var ACCEPT_ENCODING            = (cast OBJECTPOINT)   + 102;
    var PRIVATE                    = (cast OBJECTPOINT)   + 103;
    var HTTP200ALIASES             = (cast OBJECTPOINT)   + 104;
    var UNRESTRICTED_AUTH          = (cast LONG)          + 105;
    var FTP_USE_EPRT               = (cast LONG)          + 106;
    var HTTPAUTH                   = (cast LONG)          + 107;
    var SSL_CTX_FUNCTION           = (cast FUNCTIONPOINT) + 108;
    var SSL_CTX_DATA               = (cast OBJECTPOINT)   + 109;
    var FTP_CREATE_MISSING_DIRS    = (cast LONG)          + 110;
    var PROXYAUTH                  = (cast LONG)          + 111;
    var FTP_RESPONSE_TIMEOUT       = (cast LONG)          + 112;
    var IPRESOLVE                  = (cast LONG)          + 113;
    var MAXFILESIZE                = (cast LONG)          + 114;
    var INFILESIZE_LARGE           = (cast OFF_T)         + 115;
    var RESUME_FROM_LARGE          = (cast OFF_T)         + 116;
    var MAXFILESIZE_LARGE          = (cast OFF_T)         + 117;
    var NETRC_FILE                 = (cast OBJECTPOINT)   + 118;
    var USE_SSL                    = (cast LONG)          + 119;
    var POSTFIELDSIZE_LARGE        = (cast OFF_T)         + 120;
    var TCP_NODELAY                = (cast LONG)          + 121;
    var FTPSSLAUTH                 = (cast LONG)          + 129;
    var IOCTLFUNCTION              = (cast FUNCTIONPOINT) + 130;
    var IOCTLDATA                  = (cast OBJECTPOINT)   + 131;
    var FTP_ACCOUNT                = (cast OBJECTPOINT)   + 134;
    var COOKIELIST                 = (cast OBJECTPOINT)   + 135;
    var IGNORE_CONTENT_LENGTH      = (cast LONG)          + 136;
    var FTP_SKIP_PASV_IP           = (cast LONG)          + 137;
    var FTP_FILEMETHOD             = (cast LONG)          + 138;
    var LOCALPORT                  = (cast LONG)          + 139;
    var LOCALPORTRANGE             = (cast LONG)          + 140;
    var CONNECT_ONLY               = (cast LONG)          + 141;
    var CONV_FROM_NETWORK_FUNCTION = (cast FUNCTIONPOINT) + 142;
    var CONV_TO_NETWORK_FUNCTION   = (cast FUNCTIONPOINT) + 143;
    var CONV_FROM_UTF8_FUNCTION    = (cast FUNCTIONPOINT) + 144;
    var MAX_SEND_SPEED_LARGE       = (cast OFF_T)         + 145;
    var MAX_RECV_SPEED_LARGE       = (cast OFF_T)         + 146;
    var FTP_ALTERNATIVE_TO_USER    = (cast OBJECTPOINT)   + 147;
    var SOCKOPTFUNCTION            = (cast FUNCTIONPOINT) + 148;
    var SOCKOPTDATA                = (cast OBJECTPOINT)   + 149;
    var SSL_SESSIONID_CACHE        = (cast LONG)          + 150;
    var SSH_AUTH_TYPES             = (cast LONG)          + 151;
    var SSH_PUBLIC_KEYFILE         = (cast OBJECTPOINT)   + 152;
    var SSH_PRIVATE_KEYFILE        = (cast OBJECTPOINT)   + 153;
    var FTP_SSL_CCC                = (cast LONG)          + 154;
    var TIMEOUT_MS                 = (cast LONG)          + 155;
    var CONNECTTIMEOUT_MS          = (cast LONG)          + 156;
    var HTTP_TRANSFER_DECODING     = (cast LONG)          + 157;
    var HTTP_CONTENT_DECODING      = (cast LONG)          + 158;
    var NEW_FILE_PERMS             = (cast LONG)          + 159;
    var NEW_DIRECTORY_PERMS        = (cast LONG)          + 160;
    var POSTREDIR                  = (cast LONG)          + 161;
    var SSH_HOST_PUBLIC_KEY_MD5    = (cast OBJECTPOINT)   + 162;
    var OPENSOCKETFUNCTION         = (cast FUNCTIONPOINT) + 163;
    var OPENSOCKETDATA             = (cast OBJECTPOINT)   + 164;
    var COPYPOSTFIELDS             = (cast OBJECTPOINT)   + 165;
    var PROXY_TRANSFER_MODE        = (cast LONG)          + 166;
    var SEEKFUNCTION               = (cast FUNCTIONPOINT) + 167;
    var SEEKDATA                   = (cast OBJECTPOINT)   + 168;
    var CRLFILE                    = (cast OBJECTPOINT)   + 169;
    var ISSUERCERT                 = (cast OBJECTPOINT)   + 170;
    var ADDRESS_SCOPE              = (cast LONG)          + 171;
    var CERTINFO                   = (cast LONG)          + 172;
    var USERNAME                   = (cast OBJECTPOINT)   + 173;
    var PASSWORD                   = (cast OBJECTPOINT)   + 174;
    var PROXYUSERNAME              = (cast OBJECTPOINT)   + 175;
    var PROXYPASSWORD              = (cast OBJECTPOINT)   + 176;
    var NOPROXY                    = (cast OBJECTPOINT)   + 177;
    var TFTP_BLKSIZE               = (cast LONG)          + 178;
    var SOCKS5_GSSAPI_SERVICE      = (cast OBJECTPOINT)   + 179;
    var SOCKS5_GSSAPI_NEC          = (cast LONG)          + 180;
    var PROTOCOLS                  = (cast LONG)          + 181;
    var REDIR_PROTOCOLS            = (cast LONG)          + 182;
    var SSH_KNOWNHOSTS             = (cast OBJECTPOINT)   + 183;
    var SSH_KEYFUNCTION            = (cast FUNCTIONPOINT) + 184;
    var SSH_KEYDATA                = (cast OBJECTPOINT)   + 185;
    var MAIL_FROM                  = (cast OBJECTPOINT)   + 186;
    var MAIL_RCPT                  = (cast OBJECTPOINT)   + 187;
    var FTP_USE_PRET               = (cast LONG)          + 188;
    var RTSP_REQUEST               = (cast LONG)          + 189;
    var RTSP_SESSION_ID            = (cast OBJECTPOINT)   + 190;
    var RTSP_STREAM_URI            = (cast OBJECTPOINT)   + 191;
    var RTSP_TRANSPORT             = (cast OBJECTPOINT)   + 192;
    var RTSP_CLIENT_CSEQ           = (cast LONG)          + 193;
    var RTSP_SERVER_CSEQ           = (cast LONG)          + 194;
    var INTERLEAVEDATA             = (cast OBJECTPOINT)   + 195;
    var INTERLEAVEFUNCTION         = (cast FUNCTIONPOINT) + 196;
    var WILDCARDMATCH              = (cast LONG)          + 197;
    var CHUNK_BGN_FUNCTION         = (cast FUNCTIONPOINT) + 198;
    var CHUNK_END_FUNCTION         = (cast FUNCTIONPOINT) + 199;
    var FNMATCH_FUNCTION           = (cast FUNCTIONPOINT) + 200;
    var CHUNK_DATA                 = (cast OBJECTPOINT)   + 201;
    var FNMATCH_DATA               = (cast OBJECTPOINT)   + 202;
    var RESOLVE                    = (cast OBJECTPOINT)   + 203;
    var TLSAUTH_USERNAME           = (cast OBJECTPOINT)   + 204;
    var TLSAUTH_PASSWORD           = (cast OBJECTPOINT)   + 205;
    var TLSAUTH_TYPE               = (cast OBJECTPOINT)   + 206;
    var TRANSFER_ENCODING          = (cast LONG)          + 207;
    var CLOSESOCKETFUNCTION        = (cast FUNCTIONPOINT) + 208;
    var CLOSESOCKETDATA            = (cast OBJECTPOINT)   + 209;
    var GSSAPI_DELEGATION          = (cast LONG)          + 210;
    var DNS_SERVERS                = (cast OBJECTPOINT)   + 211;
    var ACCEPTTIMEOUT_MS           = (cast LONG)          + 212;
    var TCP_KEEPALIVE              = (cast LONG)          + 213;
    var TCP_KEEPIDLE               = (cast LONG)          + 214;
    var TCP_KEEPINTVL              = (cast LONG)          + 215;
    var SSL_OPTIONS                = (cast LONG)          + 216;
    var MAIL_AUTH                  = (cast OBJECTPOINT)   + 217;
    var SASL_IR                    = (cast LONG)          + 218;
    var XFERINFOFUNCTION           = (cast FUNCTIONPOINT) + 219;
    var XOAUTH2_BEARER             = (cast OBJECTPOINT)   + 220;
    var DNS_INTERFACE              = (cast OBJECTPOINT)   + 221;
    var DNS_LOCAL_IP4              = (cast OBJECTPOINT)   + 222;
    var DNS_LOCAL_IP6              = (cast OBJECTPOINT)   + 223;
    var LOGIN_OPTIONS              = (cast OBJECTPOINT)   + 224;
    var SSL_ENABLE_NPN             = (cast LONG)          + 225;
    var SSL_ENABLE_ALPN            = (cast LONG)          + 226;
    var EXPECT_100_TIMEOUT_MS      = (cast LONG)          + 227;
    var PROXYHEADER                = (cast OBJECTPOINT)   + 228;
    var HEADEROPT                  = (cast LONG)          + 229;
}
